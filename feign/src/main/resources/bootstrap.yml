spring:
  application:
    name: feign
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
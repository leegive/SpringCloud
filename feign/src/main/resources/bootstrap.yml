spring:
  application:
    name: feign
  cloud:
    config:
      fail-fast: true
      profile: dev
      label: master
#      discovery:
#        enabled: true
#        service-id: config
      uri: http://localhost:8888/
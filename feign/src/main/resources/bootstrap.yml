spring:
  application:
    name: feign
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8888/
  rabbitmq:
    host: localhost
    port: 5762
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/